# UI/UX Preferences

- **Explicit Active Filters:** When implementing visual filters on datasets (like a Color Tag filter or search bar), always provide a highly visible indicator (e.g., a red banner or top bar warning) indicating that a filter is currently active and not all items are being shown. This prevents the user from thinking data has been deleted or lost.
